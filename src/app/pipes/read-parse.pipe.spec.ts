import { ReadParsePipe } from './read-parse.pipe';

describe('ReadParsePipe', () => {
  it('create an instance', () => {
    const pipe = new ReadParsePipe();
    expect(pipe).toBeTruthy();
  });
});
